/**
 * Group related articles into stories and score them for the review queue.
 * Implementation comes after search is connected.
 */
export function groupArticlesIntoStories(articles = []) {
  return []
}

export function scoreStory(_story, _articles = []) {
  return 0
}
